name: Newman Run

on:
  push:
    branches:
    - master

jobs:
  newman:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - uses: matt-ball/newman-action@master
        with:
          collection: src/test/postman/SpringExampleCollection.postman_collection.json
